# Complete send-keys test - captures fresh hash and sends command

# Create session
tmux_new_session prefix=keytest

# Capture to get current hash
tmux_capture prefix=keytest

# Send echo command (replace HASH_HERE with hash from capture above)
# Note: Use quotes for multi-word commands
# tmux_send_keys hash=HASH_HERE keys="echo hello world" enter=true

# Capture result to see command output
# tmux_capture prefix=keytest

# Clean up
# tmux_kill prefix=keytest