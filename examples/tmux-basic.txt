# Basic tmux server test calls
# Format: tool_name arg1=value1 arg2=value2

# List all tmux sessions
tmux_list

# Create a new session with echo command
tmux_new_session command=[echo,hello,world] prefix=test

# List sessions again to see the new one
tmux_list prefix=test

# Capture the session output
tmux_capture prefix=test